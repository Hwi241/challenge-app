<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 버튼 배경색 (검정) -->
    <solid android:color="#000000" />

    <!-- 모서리 라운드 (999dp로 하면 완전 알약 모양) -->
    <corners android:radius="999dp" />

    <!-- 살짝 안쪽 여백 주고 싶으면 padding도 가능 (RemoteViews는 기기마다 다를 수 있음) -->
    <!--
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" />
    -->
</shape>
